# LocalMaxxing

This repository is an npm workspace. The public CLI package lives in `packages/localmaxxing-cli` and publishes as `localmaxxing` with `localmaxxing` and `lmx` binaries.

## CLI

```bash
npm install
npm run build:cli
npx localmaxxing --help
```
